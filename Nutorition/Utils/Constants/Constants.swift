import Firebase

let COLLECTION_FOODS = Firestore.firestore().collection("foods")
let COLLECTION_RECIPES = Firestore.firestore().collection("recipes")
let COLLECTION_INGREDIENTS = Firestore.firestore().collection("ingredients")
let COLLECTION_SPICES = Firestore.firestore().collection("spices")
let COLLECTION_STEPS = Firestore.firestore().collection("steps")
